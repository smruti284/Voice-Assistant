import React, {useMemo,useState} from "react";

const App = () => {
    const [add,setAdd] = useState(0);
    const [sub, setSub] = useState(0);

    const Mul = useMemo(() => {
        console.log("can u see problem");
    })
  return (
    <div>
      
    </div>
  )
}

export default App
